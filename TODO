Test against various versions that may have issues with requests / SSL:
    and may require urllib3 disable warnings.

better way to store authentication:  Keyring Support??

streaming transfers ...
http://docs.python-requests.org/en/master/user/quickstart/#raw-response-content

with open(filename, 'wb') as fd:
    for chunk in r.iter_content(chunk_size=128):
        fd.write(chunk)

Add input validation everywhere.

Need a test to verify __all__ imports everything.

calling  sc.get('status') on granny 2.8 fixes last four lines left in coverage!


Use 'responses' package for tests :  https://github.com/getsentry/responses
