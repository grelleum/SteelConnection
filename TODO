Test against various versions that may have issues with requests / SSL:
    and may require urllib3 disable warnings.

Create lookup sub-object (object class that is an attribute of main class)
    sconnect.lookup.orgid
    sconnect.lookup.nodeid
    sconnect.lookup.siteid
    etc.
